/**
 * @deprecated
 * Verification review is integrated into AdminDashboardPage. Keeping this
 * no-op compatibility component prevents an extracted update from leaving the
 * previous route-calling implementation behind on machines that already have
 * the old file.
 */
export default function VerificationReview() {
  return null;
}
