# Applying Phase 6

Extract this archive into the FindMyLawyer project root and allow matching files to be overwritten.

If the old root-level `AI_TEST_SCENARIOS.md` still exists, remove that duplicate after extraction because the maintained copy now lives at `docs/AI_TEST_SCENARIOS.md`.

No new npm package is required for this phase. Keep real `.env` files and secrets local; they are not included in this archive.
